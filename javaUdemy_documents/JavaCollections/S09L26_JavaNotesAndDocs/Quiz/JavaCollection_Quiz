Which method is used to append element in ArrayList? - add();

----------------------------------------------------------------------

ArrayList can contain objects only. - True

Exp: -
Because ArrayList is a collection that can only hold class objects and not 
primitive datatypes such as int, char, etc. 

The primitive datatype in java are not objects. 

They need to be converted into objects using wrapper classes. 

ArrayList cannot hold primitive data types such as int, double, char, long, etc. 
(But they can hold String, since String is an object and wrapper classes 
(Double, Integer, etc). 

So, ArrayList contains non-primitive datatype like String and Wrapper classes in 
general. 

Note: String is not a Wrapper class because there is no corresponding primitive 
type.

----------------------------------------------------------------------

String is primitive datatype. -> False -> It is a Class

----------------------------------------------------------------------

The …….. class wraps a value of the int primitive datatype in an object. -> Integer

----------------------------------------------------------------------

In Wrapper class, autoboxing and unboxing are user-defined. -> False

Autoboxing is the automatic conversion that the Java compiler makes between the 
primitive types and their corresponding object wrapper classes.

If the conversion goes the other way, this is called unboxing.

----------------------------------------------------------------------

What are the advantages of collection framework?

A. No third party API required.

B. No need to create API from handling collection objects.

C. The collections framework is tested and optimized.

----------------------------------------------------------------------

Which collection has faster read operation? -> ArrayList

----------------------------------------------------------------------

The ……….. is the collection where elements are arranged one above other. -> Stack

The stack follows LIFO (Last In First Out ) patterns. -> True
----------------------------------------------------------------------

Which method is used to add element in the stack? -> push();

----------------------------------------------------------------------

In the LinkedList, a node (not considering first node) consists of ……………..

Answer:- Current Node Data and Address of next node
----------------------------------------------------------------------

In the LinkedList, the first Node consists of ……………..

Answer:- Address of Next node
----------------------------------------------------------------------

The sort() method sorts the List in ................order.

Answer: - natural

----------------------------------------------------------------------

The compareTo method returns value -1 when both input values are the same.

Answer:- False

----------------------------------------------------------------------

The ....................... method of the ......................
interface can be used to sort Lists and arrays of objects that are instances of 
a class that implements in a natural ordering of that class.


Answer:-compareTo() , Comparable
----------------------------------------------------------------------
